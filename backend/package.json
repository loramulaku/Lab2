{
  "name": "backend",
  "version": "1.0.0",
  "description": "Job Portal API — Node.js + Express, MySQL (Sequelize) + MongoDB (Mongoose), CQRS pattern",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "db:migrate": "sequelize-cli db:migrate",
    "db:migrate:undo": "sequelize-cli db:migrate:undo",
    "db:seed": "node src/seeders/adminSeeder.js",
    "sync:retry": "node src/scripts/retrySync.js",
    "test": "jest --forceExit --runInBand",
    "test:sync": "jest --forceExit --runInBand --testPathPatterns=tests/sync",
    "test:handlers": "jest --forceExit --runInBand --testPathPatterns=tests/handlers",
    "seed:roles": "node src/seeders/seed-admin-role.js",
    "seed:admin-user": "node src/seeders/seed-admin-user.js",
    "setup": "npm run db:migrate && npm run seed:roles && npm run seed:admin-user"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 20000,
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "exceljs": "^4.4.0",
    "express": "^5.2.1",
    "json2csv": "^6.0.0-alpha.2",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.4.1",
    "multer": "^2.1.1",
    "mysql2": "^3.22.0",
    "sequelize": "^6.37.8",
    "sequelize-cli": "^6.6.5",
    "socket.io": "^4.8.3",
    "stripe": "^22.2.0"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "nodemon": "^3.1.14"
  }
}
