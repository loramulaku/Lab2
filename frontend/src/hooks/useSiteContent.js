/** Re-export from context — all consumers share one CMS state tree. */
export { useSiteContent, SiteContentProvider } from '../context/SiteContentContext';
export { useSiteContent as default } from '../context/SiteContentContext';
